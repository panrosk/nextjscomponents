// Button.stories.ts|tsx
import ClipPath from "../../components/clippath";

const meta = {
  component: ClipPath,
};

export default meta;


export const Primary = {
  args: {
    text: 'Que xopa',
    size:90
  },
};
import React from 'react';

import Rotation from "../../components/rotationclippath.jsx";

export default {
  component: Rotation,
  args: {},
}

const Template = (args) => <Rotation {...args} />;

export const Story = Template.bind({});
Story.args = {};
